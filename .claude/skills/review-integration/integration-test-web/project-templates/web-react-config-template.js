// Web React 测试项目配置文件模板
// 此文件用于测试集成文档中的配置步骤

// SDK 配置（根据文档填写）
export const SDK_CONFIG = {
  SDKAppID: 0,        // 从文档中获取
  userID: '',         // 测试用户 ID
  userSig: '',        // 从文档中获取或生成
  // 其他配置项...
};

// 初始化函数模板
export async function initSDK() {
  // 根据文档中的初始化步骤填写
  // 示例：
  // import { useLoginState } from 'tuikit-atomicx-react';
  // const { login } = useLoginState();
  // await login(SDK_CONFIG);
}
