# 📋 文档审查报告

| 项目 | 内容 |
|------|------|
| 文档路径 | `input-docs/batch_001/基础功能__分享系统声音__Mac__86735807661563904.md` |
| 文档类型 | feature |
| 审查日期 | 2026-03-13 |
| 问题总数 | 5 |
| P0（严重） | 2 |
| P1（警告） | 2 |
| P2（建议） | 1 |

---

## 📊 统计概览

| 优先级 | 数量 | 说明 |
|--------|------|------|
| 🔴 P0 严重 | 2 | 必须修复 |
| 🟡 P1 警告 | 2 | 建议修复 |
| 🔵 P2 建议 | 1 | 可选优化 |

---

## 🔴 P0 严重问题

### P0-1：API 链接错误（链接文案与目标不符 + 错误平台）

- **位置**：步骤4（开始系统声音采集）、步骤5（停止系统声音采集）、步骤6（设置系统声音采集音量）
- **问题描述**：
  1. `stopSystemAudioLoopback` 和 `setSystemAudioLoopbackVolume` 的超链接 URL 与 `startSystemAudioLoopback` 的 URL 完全相同（均为 `#a2979e32c019708dcc9209bb6d2db9486`），明显是复制粘贴错误，链接文案与目标内容完全不符。
  2. 步骤4、5、6 中所有 API 的超链接均指向 iOS API 文档路径（`group__TRTCCloud__ios` / `group__TRTCCloudDelegate__ios`），但本文档是 Mac 端文档，应指向 Mac API 文档。
- **原文**：
  - 步骤5：`stopSystemAudioLoopback` 链接 URL = `https://liteav.sdk.qcloud.com/doc/api/zh-cn/group__TRTCCloud__ios.html#a2979e32c019708dcc9209bb6d2db9486`（与步骤4 startSystemAudioLoopback URL 相同）
  - 步骤6：`setSystemAudioLoopbackVolume` 链接 URL 同上
- **修改建议**：分别为 `stopSystemAudioLoopback`、`setSystemAudioLoopbackVolume`、`onSystemAudioLoopbackError` 提供正确的 Mac API 文档链接，不得沿用 iOS API 的 URL。

---

### P0-2：回调描述与 API 命名矛盾

- **位置**：步骤4：开始系统声音采集
- **问题描述**：正文描述"接口执行完成会通过 `onSystemAudioLoopbackError` 回调成功或失败的结果"，但该回调名称带有 `Error`，语义上是错误回调。文档说它同时回调成功结果，与 API 命名存在明显矛盾，开发者无法判断成功时是否也会触发此回调。
- **原文**：`接口执行完成会通过 onSystemAudioLoopbackError 回调成功或失败的结果。`
- **修改建议**：
  - 若该回调**仅在失败时触发**，应改为："接口执行失败时，会通过 `onSystemAudioLoopbackError` 回调错误信息。"
  - 若该回调**同时覆盖成功和失败**，应补充说明：成功时的回调参数值（如错误码为 0 表示成功），以消除命名带来的歧义。

---

## 🟡 P1 警告问题

### P1-1：代码块语言标记错误（ObjC 标记为 Swift）

- **位置**：步骤4（开始系统声音采集）、步骤5（停止系统声音采集）、步骤6（设置系统声音采集音量）
- **问题描述**：三处代码块均声明语言为 `swift`，但代码内容使用 Objective-C 方括号消息发送语法（如 `[TRTCCloud sharedInstance]`、`[trtcCloud startSystemAudioLoopback]`），导致语法高亮错误，影响开发者阅读体验。
- **原文**：
  ```
  TRTCCloud *trtcCloud = [TRTCCloud sharedInstance];
  [trtcCloud startSystemAudioLoopback];
  ```
  （以上代码被标记为 `swift`）
- **修改建议**：将三处代码块的语言标记从 `swift` 改为 `objc`（或 `objective-c`）。

---

### P1-2：核心 API 参数未说明

- **位置**：步骤6：设置系统声音采集音量
- **问题描述**：`setSystemAudioLoopbackVolume` 是步骤6的核心 API，但正文仅一句话说明其用途，未说明参数的含义、类型、取值范围及默认值（示例代码中硬编码了 `80`，开发者无法判断合法范围）。
- **原文**：`调用 setSystemAudioLoopbackVolume 接口设置系统声音的采集音量。`
- **修改建议**：在正文中补充参数说明，例如："参数为整数，取值范围 0-100，0 为静音，100 为原始采集音量，默认值为 [XX]。"

---

## 🔵 P2 建议问题

### P2-1：中英文之间缺少空格

- **位置**：步骤1：集成 TRTCPrivilegedTask 库 > 手动集成 > 第3步
- **问题描述**：中文"展开"与英文"Link"之间缺少空格，不符合中英文排版规范。
- **原文**：`展开Link Binary With Libraries 项`
- **修改建议**：改为 `展开 Link Binary With Libraries 项`

---

✅ 审查完成。共发现 5 个问题（P0×2，P1×2，P2×1）。本报告无重复问题，去重删除记录为空。
