# 文档审查报告

**文档路径**: input-docs/batch_001/基础功能__开启屏幕分享__iOS__86735807331250176.md
**审查时间**: 2026-03-13
**文档类型**: feature
**L2 技能**: review-feature

---

## 问题汇总

| 编号 | 严重程度 | 类别 | 类型 | 位置 |
|------|---------|------|------|------|
| FEAT-001 | P0 | feature | 时序流程不完整 | 应用内分享 章节 |
| LOGIC-001 | P0 | logic | 步骤遗漏 | 步骤3：对接主 App 端的接收逻辑 → 第3条 |
| FORMAT-001 | P1 | format | 标题层级 | 跨应用分享 章节（步骤1-4） |
| CODE-001 | P1 | code | 代码块语言标记错误 | 步骤2/3/4 代码块 |
| LOGIC-002 | P1 | logic | 逻辑断链 | 步骤3：对接主 App 端的接收逻辑 → 第1条 |
| FEAT-002 | P1 | feature | 核心 API 参数缺少说明 | 步骤3：对接主 App 端的接收逻辑 → 第2条 |
| FEAT-003 | P1 | feature | 回调处理缺失 | 跨应用分享 → 步骤3 |
| TERMS-001 | P2 | terms | 功能概念术语不一致 | SampleHandler 代码块（步骤2）中文注释 |
| FORMAT-002 | P2 | format | 代码格式 | 跨应用分享概述列表 vs 步骤2正文 |

**总计**：P0 × 2，P1 × 5，P2 × 2，共 9 条

---

## P0 问题（严重，必须修复）

### FEAT-001 · 时序流程不完整

**位置**：应用内分享 章节

**原文**：
> 应用内分享的方案非常简单，只需要调用 TRTC SDK 提供的接口 TRTCCloud#startScreenCaptureInApp 并传入编码参数 TRTCVideoEncParam 即可。

**问题**：应用内分享章节仅说明了如何调用 `startScreenCaptureInApp` 开始屏幕分享，完全没有说明如何停止应用内屏幕分享（对应 `stopScreenCapture`），导致功能生命周期不完整，开发者无法得知如何结束分享。

**建议**：在应用内分享章节末尾补充停止说明，例如："调用 `TRTCCloud#stopScreenCapture` 可随时中止屏幕分享。"并补充对应代码示例。

---

### LOGIC-001 · 步骤遗漏

**位置**：步骤3：对接主 App 端的接收逻辑 → 第3条

**原文**：
> 等待用户触发屏幕分享。如果不实现步骤4中的"触发按钮"，屏幕分享就需要用户在 iOS 系统的控制中心，通过长按录屏按钮来触发……

**问题**：说明用户从系统控制中心触发录屏时，未说明用户需要在弹出列表中选择本 App 的扩展。对于初次接入的开发者而言，如果未在弹出列表中选择正确的扩展，屏幕分享将无法启动，这是一个容易让开发者卡住的操作步骤。

**建议**：补充说明："用户在控制中心长按录屏按钮后，需在弹出的列表中选择本 App 对应的录屏扩展，方可触发屏幕分享数据传输。"

---

## P1 问题（警告，建议修复）

### FORMAT-001 · 标题层级跳级

**位置**：跨应用分享 章节（步骤1-4 标题）

**原文**：`## 跨应用分享` 下直接使用 `#### 步骤1：创建 App Group` 等四个 H4 标题

**问题**：H2 → H4，跳过了 H3 级，违反标题必须按顺序递进（H2 → H3 → H4）的规范，影响文档层级结构的可读性。

**建议**：将步骤1~4的标题改为 H3（`###`）级别；或在 H2 下先设立一个 H3 父级标题（如"### 接入步骤"），再将步骤1~4置于其下。

---

### CODE-001 · 代码块语言标记错误

**位置**：步骤2（SampleHandler 代码块，id=46）、步骤3（startScreenCapture 代码块，id=47）、步骤4（onScreenButtonTapped 代码块，id=48）

**原文**：三个代码块均标注 `language="swift"`，但内容为 Objective-C 语法（`#import`、`@interface`、`@implementation`、`[[xxx method]:...]`、`NS*` 类型等）

**问题**：语言标记与实际代码语言不符，导致代码语法高亮完全错误，影响开发者阅读体验。

**建议**：将三处代码块的语言标记由 `swift` 改为 `objc`（或 `objective-c`）。如需同时提供 Swift 版本，可在每个步骤下新增对应的 Swift 代码块。

---

### LOGIC-002 · 逻辑断链（缺少因果说明）

**位置**：步骤3：对接主 App 端的接收逻辑 → 第1条

**原文**：
> 确保 TRTCCloud 已经关闭了摄像头采集，如果尚未关闭，请调用 TRTCCloud#stopLocalPreview 关闭摄像头采集。

**问题**：文档要求先关闭摄像头再启动屏幕分享，但未说明原因。开发者不清楚为何必须先关闭摄像头，可能跳过该步骤导致异常。

**建议**：补充原因说明，例如："由于 iOS 屏幕分享使用主流（主视频通道），与摄像头采集通道互斥，必须先停止摄像头采集才能正常启动屏幕分享。"

---

### FEAT-002 · 核心 API 参数缺少说明

**位置**：步骤3：对接主 App 端的接收逻辑 → 第2条

**原文**：
> 调用 TRTCCloud#startScreenCaptureByReplaykit:appGroup: 方法，并传入步骤1中设置的 AppGroup，让 SDK 进入"等待"状态。

**问题**：`startScreenCaptureByReplaykit:appGroup:` 是跨应用屏幕分享的核心启动 API，其第一个参数（`videoEncParam`，`TRTCVideoEncParam` 类型）在正文中未作任何说明（含义、必填/可选、传 nil 的行为）。代码示例中使用了该参数，但正文缺乏说明，不符合核心 API 参数必须在正文中说明的要求。

**建议**：在步骤3第2条正文中补充参数说明：
- `videoEncParam`（`TRTCVideoEncParam`，可选）：视频编码参数，传 `nil` 时 SDK 沿用屏幕分享前的编码参数；
- `appGroup`（`NSString`，必填）：App Group Identifier，需与步骤1中配置的值一致。

---

### FEAT-003 · 回调处理缺失

**位置**：跨应用分享 → 步骤3：对接主 App 端的接收逻辑

**原文**：代码示例仅包含 `onScreenCaptureStarted`，正文未说明屏幕分享停止或异常时的回调处理。

**问题**：代码示例仅展示了 `onScreenCaptureStarted` 成功回调，正文未说明当屏幕分享因异常终止（如 Extension 进程被系统杀死）时，主 App 端应如何感知并响应。开发者无法依据文档处理分享中断场景，影响接入质量。

**建议**：在步骤3中补充说明停止/异常回调，例如："`onScreenCaptureStopped`：屏幕分享停止时触发（包含主动停止和异常停止），回调参数包含停止原因，建议在此回调中更新 UI 状态。"

---

## P2 问题（建议，可选优化）

### TERMS-001 · 功能概念术语不一致

**位置**：SampleHandler 代码块（步骤2）中文注释字符串

**原文**：代码注释字符串 `tip = @"屏幕共享已结束"`；文档正文全文使用"屏幕分享"

**问题**：文档正文统一使用"屏幕分享"描述该功能，但 SampleHandler 代码示例中的中文字符串为"屏幕共享已结束"，两种表述混用，可能造成阅读者对概念的混淆。

**建议**：将代码注释字符串中的 `"屏幕共享已结束"` 改为 `"屏幕分享已结束"`，与全文保持一致。

---

### FORMAT-002 · 代码格式不一致

**位置**：跨应用分享章节概述列表 vs 步骤2正文第3条

**原文**：
- 概述列表：`` `TXLiteAVSDK_ReplayKitExt.framework` ``（有反引号）
- 步骤2正文第3条：`TXLiteAVSDK_ReplayKitExt.framework`（无反引号）

**问题**：同一文件名在概述列表中使用了行内代码格式，但在步骤2正文中未加反引号，格式不一致。

**建议**：将步骤2正文中的 `TXLiteAVSDK_ReplayKitExt.framework` 改为 `` `TXLiteAVSDK_ReplayKitExt.framework` ``，全文保持一致。

---

## 去重说明

本次审查所有 9 条问题均来自不同类别与位置，无重复，未执行去重删除。

---

*报告由 Claude Code 自动生成 | 审查技能: L1 × 7 + L2 review-feature | 执行时间: 2026-03-13*
